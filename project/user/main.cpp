/*********************************************************************************************************************
* 文件名称          main.cpp
* 功能说明          主入口（极简版 - 仅 WASD 键盘遥控）
* 适用平台          LS2K0300
* 修改记录
* 日期              作者                        备注
* 2026-07-25        Cline                      极简版 - WASD遥控
********************************************************************************************************************/

#include "my_global.hpp"

int main()
{
    // 初始化硬件和线程
    if (!car_init()) {
        printf("初始化失败，程序退出\n");
        return -1;
    }

    // 主循环：空闲等待，控制工作由 PIT 定时器线程完成
    while (true) {
        usleep(100000);  // 100ms，降低 CPU 占用
    }

    return 0;
}